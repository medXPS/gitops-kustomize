apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pgo-ha
  annotations:
    kubernetes.io/ingress.class: haproxy
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: adria-pg-db-pgbouncer 
            port:
              number: 5432
